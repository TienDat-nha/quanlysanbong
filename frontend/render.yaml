services:
  - type: web
    name: sanbong-frontend
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./build
    envVars:
      - key: REACT_APP_API_URL
        value: https://api-be-football.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
