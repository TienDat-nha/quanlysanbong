interface:
  display_name: "Codex Root-Cause Prompt"
  short_description: "Viet prompt fix bug tan goc sat repo"
  brand_color: "#0F766E"
  default_prompt: "Use $codex-bugfix-prompt to write one execution-ready Codex prompt that fixes a bug at the root cause, keeps cleanup narrow, and stays aligned with this sanbong frontend repo."

policy:
  allow_implicit_invocation: true
